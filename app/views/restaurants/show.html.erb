<h1><%= @restaurant.name %></h1>
<h2><%= @restaurant.address %></h2>
<h2><%= "⭐" * @restaurant.rating %></h2>
<%= link_to "Delete", restaurant_path(@restaurant.id), data: { turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
<%= link_to "See all restaurants", restaurants_path %>
<%= link_to "Edit", edit_restaurant_path(@restaurant.id) %>